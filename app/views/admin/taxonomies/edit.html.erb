<div class="col-md-10">
  <%= render_box_header @taxonomy, title: 'Edit taxonomy: ' + @taxonomy.name, button: 'Add new', icon: 'plus' %>

  <div class="content-box-large box-with-header">
    <%= render 'form' %>
  </div>
</div>
