# To use the locales, use `I18n.t`:
#     I18n.t 'hello'
# In views, this is aliased to just `t`:
#     <%= t('hello') %>
en:
  invalid:
    email: invalid format
    password: is too weak. Must contain 8 or more characters, a digit,
      a lower case character, an upper case character, a symbol
